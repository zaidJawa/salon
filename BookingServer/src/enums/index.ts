export enum Role {
    user = 'user',
    admin = "admin",
    super_admin = 'super_admin'
}

export enum Status {
    pending = "pending",
    approved = "approved",
    completed = "completed",
    cancelled = "cancelled",
}